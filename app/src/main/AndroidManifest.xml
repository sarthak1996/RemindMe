<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sarthak.remindme">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity android:name=".WelcomePages"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".ViewReminder"
            android:parentActivityName=".MainActivity"
            android:theme="@style/ViewReminderTheme"></activity>
        <activity android:name=".RemindMeAgainNotification"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".RecycleBin"
            android:theme="@style/AppTheme"></activity>
        <receiver android:name=".NotificationAlarm" />
    </application>

</manifest>